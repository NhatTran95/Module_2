package practice.testing;

public class Lythuyet {
    //    TDD là viết tắt của Test Driven Development - phát triển (mà trong đó sự phát triển) được lái bởi kiểm thử, là một
    // phương pháp cải tiến cho các nhà phát triển phầm mềm chuyên nghiệp, giúp nâng cao năng suất và hiệu quả phầm mềm
    //    TFD - Test-First Development (Phát triển kiểm thử đầu tiên - tức là bạn cần phải viết ra các trường hợp kiểm thử
    // trước khi viết mã lệnh)
    //    Refactoring (Tái cấu trúc – thay đổi cấu trúc đoạn mã sau khi các kiểm thử được thực thi để cải tiến đoạn mã tốt hơn).
    //    TDD bắt đầu bằng việc chú ý vào mục tiêu đầu tiên là viết một kiểm thử cho chương trình. Khi thực thi, kiểm thử sẽ
    // thất bại. Bước tiếp theo nhà phát triển chú ý vào mục tiêu vượt qua kiểm thử. Tại bước tiếp theo, nhà phát triển
    // đặt mục tiêu tái cấu trúc để chương trình có mã tốt hết mức có thể
    //    Unit testing, còn được gọi là Component testing, mức độ kiểm thử phần mềm này thường do Developer phụ trách, họ sẽ
    // đi kiểm tra các module, các hàm, các phương thức, các lớp,… mà họ viết ra nhằm gia tăng sự tin cậy cho các chức năng mà mình viết.
}
